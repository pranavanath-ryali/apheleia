/// A [`NodeData`] for a specific node stores its relative position and size.
/// This is split from extensions because the framework works on atleast a position and
/// size for a given node.
pub struct NodeData {
    
}
